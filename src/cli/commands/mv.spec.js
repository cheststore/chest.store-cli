import assert from 'assert'
// import Mv from './mv'

// const heartbeat = Mv({ log: {}})

// describe("Mv", function() {
//   describe("#getCommandFactory", function() {
//     it("should take an invalid command and return null for the factory", function() {
//       const inv1 = heartbeat.getCommandFactory(`invalid`)
//       const inv2 = heartbeat.getCommandFactory(`invalid.command`)
//       assert.equal(null, inv1)
//       assert.equal(null, inv2)
//     })

//     it("should take valid commands and return the actual factory back for the factory", function() {
//       const val1 = heartbeat.getCommandFactory(`ping`)
//       const val2 = heartbeat.getCommandFactory(`nessus.scan.download`)
//       assert(val1)
//       assert(val2)
//     })
//   })
// })
